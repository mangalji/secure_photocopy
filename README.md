Welcome to my project.....

for using this project in your system follow some steps:

step 1: 
    git clone https://github.com/mangalji/secure_photocopy

step 2:
    create the virtual environment:
        pip install venv
        python3 -m venv venv
        source venv/bin/activate
        pip install -r requirements.txt
    

